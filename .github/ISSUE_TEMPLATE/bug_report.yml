name: Bug Report
description: Report a bug or unexpected behavior in connectivity_control
labels: ["bug", "needs: triage"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for taking the time to report a bug! Please fill in as much detail as possible so we can reproduce and fix the issue.

  - type: checkboxes
    id: preflight
    attributes:
      label: Pre-flight checklist
      options:
        - label: I have searched existing issues and this is not a duplicate
          required: true
        - label: I have updated to the latest version of the package and the bug still occurs
          required: true

  - type: dropdown
    id: platform
    attributes:
      label: Platform
      description: Which platform(s) does this affect?
      multiple: true
      options:
        - Android
        - iOS
    validations:
      required: true

  - type: textarea
    id: description
    attributes:
      label: Bug description
      description: A clear and concise description of what the bug is.
      placeholder: "When I call getActiveNetworks(), the result is empty even though..."
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: Steps to reproduce
      description: Step-by-step instructions to reproduce the behavior.
      placeholder: |
        1. Call `ConnectivityControl().getActiveNetworks()`
        2. ...
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: Expected behavior
      description: What did you expect to happen?
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: Actual behavior
      description: What actually happened?
    validations:
      required: true

  - type: textarea
    id: code
    attributes:
      label: Minimal reproducible code
      description: Paste the smallest snippet that demonstrates the issue.
      render: dart

  - type: textarea
    id: logs
    attributes:
      label: Logs / stack trace
      description: Paste any relevant logs or crash output here.
      render: shell

  - type: input
    id: package_version
    attributes:
      label: Package version
      placeholder: "e.g. 1.1.0"
    validations:
      required: true

  - type: input
    id: flutter_version
    attributes:
      label: Flutter version
      description: Output of `flutter --version`
      placeholder: "e.g. Flutter 3.27.0, Dart 3.6.0"
    validations:
      required: true

  - type: input
    id: os_version
    attributes:
      label: OS / device
      description: OS name, version, and device/emulator type
      placeholder: "e.g. iOS 17.4 (iPhone 15 simulator), Android 14 (Pixel 8)"
    validations:
      required: true

  - type: textarea
    id: additional
    attributes:
      label: Additional context
      description: Any other context, screenshots, or information that might help.
